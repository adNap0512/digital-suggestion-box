# ループエンジニアリングとは

発表用の解説資料（概要は約3分）  
詳細な実施記録は [Report.md](./Report.md) を参照する。

| 資料 | 役割 |
|------|------|
| **この資料** | 思想・特徴・メリット・始め方 |
| [Report.md](./Report.md) | デジタル目安箱で回した Iteration 1〜5 と学び |

この資料のあとで Report.md の GOAL.md / progress.md / Rules / Skills / STOP / Iteration を見ると、「なぜその仕組みを置いたか」がつながる。

---

## 発表3分の目次

1. 一言でいうと
2. 新人の例
3. カーナビの例
4. 従来との最大の違い
5. メリット
6. 始めるための7要素
7. 今回どう使ったか
8. Report.md の Iteration 1〜5 へ

---

## 1. 一言でいうと

ループエンジニアリングは、

**AI に毎回細かい実装手順を指示するのではなく、Goal に向かって「状態確認 → 次の作業判断 → 実行 → 検証 → 状態更新」を繰り返せる環境そのものを設計する考え方**

である。

「AI を自動実行する方法」ではない。  
「プロンプトが不要になった」でもない。

設計する範囲が広がっていく、というイメージ。

```txt
Prompt Engineering
「1回の指示をどう良くするか」

       ↓ 捨てるのではなく、上位でも使う

Context Engineering
「AIが判断するために何を見せるか」

       ↓

Harness Engineering
「Rules / Tools / Test など、
 AIが安全に作業できる環境をどう作るか」

       ↓

Loop Engineering
「AIが状態を見ながら、
 判断 → 実行 → 検証を繰り返せる
 仕組み全体をどう作るか」
```

ループの中でも、プロンプト・コンテキスト・Rules・テストは使い続ける。広がるのは「誰が次の一手を決めるか」と「何周も回せる土台があるか」である。

---

## 2. 例え：新人へ仕事を依頼する

従来は、手順を細かく出す。

```txt
上司：
Excelを開いて
↓
このデータをコピーして
↓
グラフを作って
↓
PDFにして
```

作業が終わるたびに、「次は何をすればいいですか？」となる。

AI 開発に近い形：

```txt
人間 → このファイルを作って → AI
     → 次にこの関数を書いて → AI
     → 次にテストを書いて   → AI
```

ループでは、**手順ではなく目的・完成条件・ルールを渡す**。

```txt
目的：
明日の会議資料を完成させる

完成条件：
・必要なデータがある
・グラフがある
・誤字がない
・PDF化できる

ルール：
・元データは変更しない
・判断できない場合は確認する
```

新人（AI）は、いま何が足りないかを見て、次の最小作業を自分で選ぶ。上司（人間）は、目的と止まり方を設計する。

---

## 3. 例え：カーナビ

従来：

```txt
次の信号を右
↓
300m直進
↓
次を左
```

人間が毎回「次に何をするか」を指示する。

ループ：

```txt
目的地：東京駅
```

を設定すると、ナビ側が繰り返す。

```txt
現在地確認
↓
ルート判断
↓
進む
↓
現在地再確認
↓
必要ならルート修正
↓
目的地到着
```

ポイントは同じである。

**「次に何をするか」を毎回人間が決めるのではなく、現在状態から判断する。**

目的地を間違えてセットすれば迷走する。Goal の設計が、そのまま品質になる。

---

## 4. 従来との一番大きな違い

観点はひとつ。**次の作業を誰が判断するか。**

従来：

```txt
人間
 ↓ 次の作業を決める
AIへ指示
 ↓
AIが実装
 ↓
人間が確認
 ↓
人間がまた次を決める
```

ループエンジニアリング：

```txt
人間
 ↓ Goal / 制約 / 完了条件を設計
AI
 ↓ 現在状態を確認
 ↓ 次の作業を判断
 ↓ 実行
 ↓ 検証
 ↓ 状態記録
 ↓ Goal未達なら、また次を判断
```

人間が不要になるわけではない。むしろ、

- Goal を書く
- やってはいけないことを書く
- 成功の測り方を書く
- 止める条件を書く
- 大きい判断は自分でする

が人間の仕事になる。

---

## 5. cron / while との違い

「Loop なら普通の繰り返しでは？」への答え。

普通の自動処理：

```txt
同じ処理 → 同じ処理 → 同じ処理
```

ループエンジニアリング：

```txt
状態を見る
 ↓
AIが次の作業を判断
 ↓
必要な処理を実行
 ↓
結果を見る
 ↓
次の行動を再判断
```

繰り返す対象は、固定のコマンドではなく **「判断を含む仕事のサイクル」** である。

---

## 6. メリット

### 1. 細かな指示を繰り返す必要が減る

人間は実装手順より、Goal・制約・完了条件の設計に集中できる。

### 2. AI 自身が実装結果を検証できる

```txt
実装 → テスト → 失敗 → 原因分析 → 修正 → 再テスト
```

「できた」ではなく、テストやビルドで判定する。

### 3. 作業を再開しやすい

チャット履歴だけに頼らない。`progress.md` などに、現在状態・完了内容・次の候補・テスト結果・判断を残す。次の起動で長い説明をやり直さなくてよい。

### 4. 再現性を高められる

担当者ごとに違う長いプロンプトを書く代わりに、Goal / Rules / Skills / Tests / STOP をプロジェクト側に置く。

### 5. 人間へ判断を返せる

何でも AI に決めさせない。大きな設計、外部サービス、Secret、共通 PF 仕様などは、

```txt
STOP → 選択肢と判断材料を出す → 人間が決める
```

ように設計する。

---

## 7. 始めるための7要素

これを揃えると試せる、という最小セット。

| # | 要素 | 何のため |
|---|------|----------|
| 1 | **Goal** | 何を達成すれば終了か |
| 2 | **Acceptance Criteria** | 成功を客観的に判定する条件 |
| 3 | **Rules** | 作業中に守る制約 |
| 4 | **Skill / 手順** | 1 イテレーションの進め方 |
| 5 | **Memory / State** | 前回の状態の置き場（例: `progress.md`） |
| 6 | **Verification** | test / build / lint など機械判定 |
| 7 | **STOP Conditions** | いつ止めて人間へ返すか |

「完成しました」という自己申告だけではループにしない。

---

## 8. 注意点

- Goal が曖昧だと迷走する
- 機械的な成功判定がないと、できた気になって進む
- STOP 条件がないと、Secret や大きな設計まで独走しやすい
- 大きすぎる Goal は小さく分ける（今回は A と B）
- 無制限に回さない（今回は 1 起動 = 1 周）
- 止まったあとも、人間がレビューする地点は残す

---

## 9. 今回のデジタル目安箱での対応

題材は「投稿データの境界と永続化 PoC」。目的はバックエンド完成ではなく、**この開発方法が回るか** の検証。

| 役割 | 今回の実体 |
|------|------------|
| Goal / 完了条件 | [GOAL.md](./GOAL.md)（Goal A / B、対象外、受け入れ条件） |
| 状態管理 | [progress.md](./progress.md)（Iteration、Next Task、Verification、Stop Reason など） |
| 常時制約 | `.cursor/rules/`（通常はバックエンド禁止） |
| 実験中の許可 | `.cursor/rules/backend-poc-rule.mdc`（GOAL 明示時のみ、範囲内だけ許可。上書きではない） |
| 1 イテレーションの手順 | `.claude/skills/loop-engineering/SKILL.md`（手動起動、`disable-model-invocation: true`） |
| 検証 | `npm test -- --run` と `npm run build`（Vitest / Vite） |
| 人間との境界 | Skill の STOP 条件 |

通常の `project-rule.mdc` はバックエンド禁止のまま。`backend-poc-rule.mdc` はそれを常時解除しない。GOAL.md に実験が明示され、現在 Goal の範囲内で、progress.md と矛盾しないときだけ許可する。

1 周の中身（Skill どおり）:

```txt
GOAL.md を読む
 ↓
progress.md を読む
 ↓
Goal 達成なら止まる
 ↓
未達なら次の作業を1つだけ決める
 ↓
テスト → 最小実装 → test / build
 ↓
progress.md を更新して STOP
```

人間は最初から「Repository を作れ」「Web Storage を使え」「Context をこう変えろ」と手順を指定していない。Goal・制約・成功条件・STOP を置いた結果、AI が順序を判断した。

---

## 10. 実験のあらすじ（詳細は Report.md）

環境構築のあと、実装は 5 周。

```txt
Iteration 1
Repository 境界（契約 + Memory。Context 未接続）

 ↓
Iteration 2
Context 接続 → Goal A 達成 → 停止

 ↓
Iteration 3
永続化方式は大きな判断
 → 実装せず STOP
 → 比較を人間へ返却
 → 人間が Web Storage を PoC として承認

 ↓
Iteration 4
Persistent Repository（Context 未接続）

 ↓
Iteration 5
Context 接続 → Goal B 達成 → 停止
（次の Goal へは進まない）
```

Iteration 3 が、この考え方の見せ場である。自律性は「勝手に実装を続けること」ではなく、**人間判断が必要な場所で止まれること** でもある。

各周のファイル名・テスト件数・構成図は [Report.md](./Report.md) へ。

---

## 11. 今回の実体験から伝えたいこと

従来は、**「AI にどう指示するか」** の比重が大きかった。

今回ループを試して、**「AI が正しく判断できる環境をどう設計するか」** へ、人間の役割が動くことを実感した。

ただし「AI に全部任せればよい」ではない。

人間はむしろ、

- Goal を決める
- 制約を書く
- 成功条件を機械判定できる形にする
- STOP 条件を置く
- 大きい意思決定をする

必要がある、というのがいまの学びである。

次に Report.md を見るときは、個別のコミット内容より、

「なぜ GOAL と progress と STOP を先に置いたのか」

をこの資料と重ねて読んでほしい。

---

## 参考

考え方の整理にあたり、次の記事を参考にした。  
記事の転載ではなく、その視点と、デジタル目安箱で実際に回した内容を組み合わせている。

- Simon Zhang, 「[Harness Engineering から Loop Engineering へ ― AI 駆動開発の次の段階](https://qiita.com/Simon_Zhang/items/68d5f31bfaec6e8ac748)」, Qiita
