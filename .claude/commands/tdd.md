# /tdd

> 学習用（旧形式）。実務では `.claude/skills/tdd/SKILL.md`（`/tdd`）を優先してください。

TDD を意識して、以下の順番で進めてください。

1. 今から実装する小さな単位を決める
2. 先にテスト観点を書く
3. テストコードを作成する
4. 最小限の実装をする
5. テストを実行する
6. 必要ならリファクタリングする

制約:

- 一度に大きく作りすぎない
- 主要な表示項目、フォーム入力、フィルタ、共感ボタンを必ずテスト対象にする
- カバレッジ 80% 以上を目指す
- テストが通っていない状態で完了しない
