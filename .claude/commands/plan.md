# /plan

> 学習用（旧形式）。実務では `.claude/skills/planning/SKILL.md`（`/planning`）を優先してください。

以下の順番で、実装前の計画を作成してください。

1. 画面構成案
2. コンポーネント構成案
3. データ構造案
4. テスト観点
5. 実装手順

制約:

- まだ実装しない
- DESIGN.md を参照する
- .cursor/rules の内容を守る
- バックエンドは作らない
- ダミーデータで成立するモックにする
- TDD を意識した手順にする
